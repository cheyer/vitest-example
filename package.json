{
  "name": "vitest-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "test:coverage:ci": "vitest run --coverage --reporter=json --outputFile ./coverage/report.json && ./scripts/save-coverage-file.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-v8": "^1.3.1",
    "vitest": "^1.3.1"
  }
}